# ChatAssist — frontend-ის React-ზე გადაყვანა (სამუშაო სპეციფიკაცია / პრომტი)

> ეს დოკუმენტი არის **პრომტი Claude Code-სთვის**. თითო ფაზა ცალკე უნდა შესრულდეს,
> ბოლოს smoke-ტესტით. არ გადახვიდე შემდეგ ფაზაზე, სანამ წინა არ დამოწმდა.

**კონტექსტი:** vanilla JS frontend (4,165 ხაზი), FastAPI backend, Supabase auth,
Render hosting. build-სისტემა **არ არსებობს** — FastAPI პირდაპირ ასერვირებს `/panel`-ზე.

---

## 🔴 ნაწილი 1 — URL-კონტრაქტი (ხელშეუხებელი)

ეს URL-ები **რეგისტრირებულია კოდის გარეთ** — Meta-ს დაფაზე, Supabase-ზე, და
**უკვე გაგზავნილია რეალურ კლიენტებთან**. თუ რომელიმე შეიცვლება, გატყდება
ისეთი რამ, რასაც კოდში ვერ დაინახავ.

| URL | სად არის რეგისტრირებული | რა გატყდება |
|---|---|---|
| `/panel/order.html?shop=ID` | `backend/app/services/bot.py:171` — **ბოტი ამ ლინკს რეალურ მყიდველებს უგზავნის Messenger-ში** | ⚠️ ყველა შეკვეთის ლინკი, რაც ოდესმე გაიგზავნა |
| `/panel/delete-data.html?code=X` | `backend/app/api/facebook.py:178` + Meta App Settings | Meta-ს data-deletion შესაბამისობა |
| `/panel/privacy.html` | Meta App Settings → Privacy Policy URL | **App Review** |
| `/panel/terms.html` | Meta App Settings → Terms of Service URL | **App Review** |
| `/panel/reset.html` | Supabase → Auth → Redirect URLs | პაროლის აღდგენა |
| `/panel/landing.html` | `backend/app/main.py:84` (root redirect) | მთავარი გვერდი |
| `/panel/index.html` | პანელი | გამყიდველის პანელი |
| `/panel/admin.html` | ადმინი | ადმინ-პანელი |

### 👉 აქედან გამომდინარე მთავარი არქიტექტურული გადაწყვეტილება

**გამოიყენე Vite-ის MPA (multi-page) რეჟიმი, არა SPA client-side routing.**

ტიპური React SPA აკეთებს `/order`, `/privacy` ტიპის „სუფთა" მისამართებს — **ეს
გატეხავს ზემოთ ჩამოთვლილ ყველაფერს.** build-ის გამოსავალი უნდა იყოს **ზუსტად
იგივე `.html` ფაილები, იგივე სახელებით, იგივე გზაზე.**

```js
// vite.config.js — build.rollupOptions.input
{
  landing:     'landing.html',
  index:       'index.html',
  admin:       'admin.html',
  order:       'order.html',
  reset:       'reset.html',
  privacy:     'privacy.html',
  terms:       'terms.html',
  'delete-data': 'delete-data.html',
}
```

**მისაღებობის კრიტერიუმი:** build-ის შემდეგ `dist/`-ში უნდა იყოს ზუსტად ეს 8
`.html` ფაილი, იგივე სახელებით.

---

## 🔴 ნაწილი 2 — რა არ უნდა შეიცვალოს

1. **Backend — ნულოვანი ცვლილება.** არც ერთი endpoint, არც ერთი როუტი.
   ერთადერთი დასაშვები: `main.py`-ში static mount-ის საქაღალდე (`frontend` → `frontend/dist`).
2. **API-კონტრაქტი** — 38 endpoint უცვლელი რჩება.
3. **Supabase-ის auth კონფიგი — განსაკუთრებით `detectSessionInUrl: false`.**
   ეს განზრახაა: Facebook-ის redirect-ის `#_=_` რომ არ დაარღვიოს სესია.
   თუ ეს ამოაგდე, FB-ის დაკავშირება გატყდება.
4. **Token-ის ლოგიკა** `app.js`-იდან (`readStoredToken`, `getToken`, `refreshToken`,
   `api()`-ის 401-retry) — **ზუსტად გადაიტანე, არ „გააუმჯობესო".** დახვეწილი ლოგიკაა.
5. **`config.js`** — `API_BASE` runtime-ზე გამოითვლება `location.origin`-იდან.
   არ გადაიყვანო build-time env-ცვლადზე; იგივე ქცევა შეინარჩუნე.
6. **ღილაკების სახელები და ნაკადი** — App Review-ს test steps ეყრდნობა
   „Connect Facebook Page"-ს. ტექსტი არ გადაარქვა.

---

## 🧨 ნაწილი 3 — მყიფე ადგილები კოდში (ყველაზე მაღალი რისკი)

ეს ის ადგილებია, სადაც React-ის „ჩვეულებრივი" წესით წერა **ჩუმად ტეხს** ფუნქციას —
შეცდომას არ დაწერს, უბრალოდ აღარ იმუშავებს.

### 3.1 ⭐ Facebook-ის დაკავშირება — popup + postMessage (ყველაზე კრიტიკული)

ეს არის **ზუსტად ის ნაკადი, რომელიც App Review-ს ვიდეოშია.** თუ გატყდა, უარყოფა გარანტირებულია.

როგორ მუშაობს დღეს:
1. `frontend/app.js:784` — `window.open("about:blank", "fbconnect", ...)` იხსნება
   **დაუყოვნებლივ, click-ის დამმუშავებელში**, მერე ხდება ნავიგაცია.
   ⚠️ ეს განზრახაა: თუ `window.open`-მდე `await` გაკეთდა, **ბრაუზერი popup-ს დაბლოკავს.**
2. `frontend/app.js:797` — `window.addEventListener("message", ...)` რეგისტრირდება
   **მოდულის ჩატვირთვისთანავე** (ადრე, ყველაფერზე უწინ).
3. `backend/app/api/facebook.py:_finish()` — popup აგზავნის `postMessage`-ს
   **კონკრეტულ origin-ებზე** (არა `*` — უსაფრთხოებისთვის), მერე თავს იხურავს.
4. თუ popup დაბლოკილია → fallback: `{frontend_url}/?fb=...` query-პარამეტრებით,
   რომელსაც `handleFbRedirect()` (`app.js:822`) კითხულობს.

**React-ის საფრთხე:**
- `useEffect`-ში ჩასმული `message`-listener რეგისტრირდება **mount-ის შემდეგ** —
  თუ popup მანამდე მოასწრებს პასუხს, **შეტყობინება იკარგება** და პანელი
  ვერასდროს გაიგებს, რომ გვერდი დაუკავშირდა. შეცდომა არ დაიწერება.
- **React StrictMode dev-ში effect-ებს ორჯერ უშვებს** (add → remove → add).
  თუ შეტყობინება „remove"-ის მომენტში მოვიდა — დაიკარგა.

**წესი:**
- `message`-listener დაარეგისტრირე **React-ის გარეთ, მოდულის დონეზე** (როგორც ახლაა),
  ან app-ის root-ში `useEffect`-ით + **ბუფერით** ადრე მოსული შეტყობინებებისთვის.
- `window.open` დარჩეს **სინქრონული** click-handler-ში. ❌ არ დაუწერო `await` წინ.
- origin-ის შემოწმება უცვლელად გადაიტანე.
- ✅ **მისაღებობა:** გვერდის დაკავშირება მუშაობს **ორივე გზით** — popup-ითაც და
  popup-დაბლოკილის fallback-ითაც (`?fb=...`).

### 3.2 სტატიკური ფაილები, რომლებსაც Vite ავტომატურად არ გადაიტანს

| ფაილი | სად გამოიყენება | თუ დაიკარგა |
|---|---|---|
| `sample-products.xlsx` | `index.html:196` — „Excel ნიმუში" ჩამოტვირთვა | ლინკი 404 |
| `sample-products.csv` | `index.html:197` — „CSV ნიმუში" | ლინკი 404 |
| `favicon.svg` | ყველა გვერდი | ტაბის აიკონი ქრება |
| `config.js` | `<script src="config.js">` — `window.APP_CONFIG` | 🔴 **აპი საერთოდ არ ჩაიტვირთება** |

**წესი:** ოთხივე ჩადე `frontend/public/`-ში — Vite მას უცვლელად აკოპირებს `dist/`-ში.
`config.js` განსაკუთრებით: ის build-ის გარეშე უნდა რჩებოდეს რედაქტირებადი.

### 3.3 dev-პორტის დაშვება

`frontend/config.js` ითვლის: `location.port === "5500" ? "http://localhost:8000" : location.origin`.
Vite dev-სერვერი **5173**-ზე დგება → dev-ში API_BASE არასწორად გამოითვლება.

**წესი:** dev-ის შემთხვევა დაამატე (5173), **პროდაქშენის ქცევა კი უცვლელი დატოვე**
(`location.origin`). ასევე განაახლე `start-frontend.ps1` / `frontend/serve.py`.

### 3.4 ⚠️ FB-დაკავშირების ტესტი ცოცხალ გვერდზე

smoke-ტესტის #6 („Connect Facebook Page") **რეალურ გვერდზე** სრულდება და
**გვერდის token-ს ხელახლა გასცემს**. ეს არ არის უვნებელი:
- ⛔ **არ გაიმეორო App Review-ს მიმდინარეობისას**
- გაუშვი მხოლოდ ფაზა 6-ის ბოლოს, გაცნობიერებულად, და მერე დაადასტურე,
  რომ ბოტი Messenger-ში ისევ პასუხობს

---

## 🔐 ნაწილი 4 — უსაფრთხოება (build-ის გამო ახალი რისკი)

`dist/` git-ში ჩადის — ე.ი. **build-ის გამოსავალი საჯარო ხდება.** გადაამოწმე:

1. ❌ `.env`-ის ან სერვერული საიდუმლოების კვალი `dist/`-ში **არასდროს**.
   push-ამდე გაუშვი: `grep -rin "service_role\|FB_APP_SECRET\|GEMINI\|FERNET" frontend/dist/`
   → შედეგი უნდა იყოს **ცარიელი**.
2. Vite ბრაუზერში ამჟღავნებს **მხოლოდ `VITE_`-პრეფიქსიან** ცვლადებს.
   არც ერთი სერვერული საიდუმლო არ დაარქვა `VITE_`-ით.
3. `config.js`-ში მხოლოდ **`SUPABASE_ANON_KEY`** (საჯაროა, RLS იცავს) — `service_role` არასდროს.
4. `package-lock.json` **ჩააკომიტე** — რომ build განმეორებადი იყოს.

---

## ⚠️ ნაწილი 5 — ფარული ხაფანგი: Render-ს Node არ აქვს

რეპოში **არც `render.yaml` არის, არც `Dockerfile`** — ე.ი. Render კონფიგურირებულია
დაფიდან და ეს არის **Python-სერვისი**. Python-runtime-ს **Node.js არ მოყვება**,
ანუ `npm run build` სერვერზე **ვერ გაეშვება**.

**გადაწყვეტა (ამ პროექტისთვის რეკომენდებული):**
- **ვარიანტი A ⭐ — `dist/` ჩააკომიტე git-ში.** build-ს ლოკალურად აკეთებ push-ამდე.
  სერვერზე ინფრასტრუქტურა **საერთოდ არ იცვლება** → ყველაზე დაბალი რისკი.
  საჭიროა `.gitignore`-იდან `dist/`-ის მოხსნა.
- ვარიანტი B — `Dockerfile` (node build stage + python stage). უფრო სუფთა, მაგრამ
  deploy-ის მთელი კონფიგი იცვლება. **მიგრაციის დროს არ ღირს.**

**გადაამოწმე Render-ის დაფაზე,** სანამ ფაზა 7-ს დაიწყებ.

---

## 📋 ნაწილი 6 — ფაზები

რიგი შერჩეულია **რისკის ზრდის მიხედვით**: ჯერ ყველაზე პატარა და უსაფრთხო.

### ფაზა 0 — უსაფრთხოების ბადე
- [ ] `git checkout -b react-migration` (main ხელუხლებელი რჩება)
- [ ] ჩაწერე **ამჟამინდელი** ქცევის smoke-ჩექლისტი (იხ. ნაწილი 7) — გაუშვი და დააფიქსირე რომ ყველაფერი მუშაობს
- [ ] ⛔ **App Review-ს მიმდინარეობისას არაფერი დაპუშო `main`-ზე**

### ფაზა 1 — მხოლოდ tooling (ქცევა არ იცვლება)
- [ ] `frontend/package.json` + Vite + React + `@supabase/supabase-js` (CDN-იდან npm-ზე)
- [ ] `vite.config.js` — MPA, 8 entry (იხ. ნაწილი 1), `base: '/panel/'`
- [ ] `main.py` — static mount `frontend/dist`-ზე (dev-ში `frontend`)
- [ ] **არც ერთი გვერდი ჯერ არ გადაგვყავს React-ზე** — Vite უბრალოდ აბანდლებს არსებულ HTML/JS-ს
- [ ] ✅ **მისაღებობა:** ყველა 8 URL იხსნება, საიტი იქცევა **ზუსტად ისე, როგორც ადრე**

### ფაზა 2 — `reset.html` (72 ხაზი) — pipeline-ის შემოწმება
- [ ] ყველაზე პატარა გვერდი → React კომპონენტად
- [ ] ✅ პაროლის აღდგენა Supabase-ის ლინკიდან **ბოლომდე მუშაობს**

### ფაზა 3 — `order.html` (171 ხაზი) — ⚠️ საჯარო, კლიენტებისთვის
- [ ] React-ზე; **URL-პარამეტრი `?shop=ID` უცვლელი**
- [ ] ✅ ბოტის გაგზავნილი ძველი ლინკი მუშაობს; შეკვეთა ბაზაში ჩადის

### ფაზა 4 — სტატიკური გვერდები → ⛔ გაუქმდა (გადაწყვეტილება 2026-08-20)
`landing.html`, `privacy.html`, `terms.html` შეიცავს **0 ხაზ JavaScript-ს**,
`delete-data.html` — 8 ხაზს. React-ზე გადაყვანა თითოეულს **~190 kB**-ს დაამატებდა
სანაცვლოდ არაფრის გარეშე.

რატომ არა:
- `landing.html` მარკეტინგული გვერდია — ჩატვირთვის სიჩქარე კონვერსიაზე მოქმედებს
- `privacy` / `terms` / `delete-data` — **სწორედ ესენი გახსნის Meta-ს განმხილველი**;
  რაც მარტივია, მით ნაკლები რისკი
- Vite მათ **უკვე ამუშავებს** (CSS-ჰეშირება, ასეტები) — მიგრაციის სარგებელი მიღებულია

**წესი მომავლისთვის:** სტატიკური გვერდი სტატიკური რჩება. React მხოლოდ იქ,
სადაც რეალური ინტერაქცია და state-ია (პანელი, ადმინი, შეკვეთის ფორმა).

### ფაზა 5 — `admin.html` (383 ხაზი)
- [ ] React-ზე, ადმინის ავტორიზაციით
- [ ] ✅ sellers / overview / growth / upgrade-requests მუშაობს

### ფაზა 6 — `index.html` + `app.js` (1,422 ხაზი) — ⭐ ყველაზე დიდი, ბოლოს
დაშალე კომპონენტებად (**არა ერთ გიგანტურ კომპონენტად**):
- [ ] `Auth` (login/register/reset) · `Dashboard` (სტატები) · `Products` (CRUD + ფოტო)
- [ ] `Import` (Excel preview/mapping) · `Orders` · `Attention` (handoff) · `Facebook` (connect/disconnect)
- [ ] `Knowledge` · `Usage/Billing` (tier gates)
- [ ] state: `shops`, `currentShopId`, `allProducts` → Context ან 2-3 hook (Redux **არ გჭირდება**)
- [ ] ✅ **მისაღებობა:** მთელი smoke-ჩექლისტი (ნაწილი 7) გადის

### ფაზა 7 — deploy
- [ ] `dist/` ამოიღე `.gitignore`-იდან, ჩააკომიტე build
- [ ] **build-ის გამოსავლის ავტო-შემოწმება:** დაადასტურე, რომ `dist/`-ში არის
      ზუსტად 8 `.html` + `config.js` + `favicon.svg` + 2 sample-ფაილი
- [ ] საიდუმლოების grep-შემოწმება (ნაწილი 4.1) — ცარიელი შედეგი
- [ ] Render-ზე შეამოწმე რომ საიტი მუშაობს
- [ ] ძველი ფაილები წაშალე **მხოლოდ მას შემდეგ**, რაც ლაივზე დადასტურდება

---

## ✅ ნაწილი 7 — Smoke-ჩექლისტი (ყოველი ფაზის შემდეგ)

1. `/` → გადამისამართდება landing-ზე
2. რეგისტრაცია → შესვლა → გამოსვლა
3. პაროლის აღდგენა (Supabase-ის ლინკი → `/panel/reset.html`)
4. მაღაზიის შექმნა → პროდუქტის დამატება (ფოტოთი) → რედაქტირება → წაშლა
5. Excel-იმპორტი: preview → mapping → confirm
6. **„Connect Facebook Page" → ნებართვები → გვერდი დაკავშირდა** ⭐
7. Messenger-ში მიწერე → **ბოტი პასუხობს** ⭐
8. შეკვეთის ლინკი (`/panel/order.html?shop=ID`) → შეკვეთა ბაზაში ჩადის
9. „ყურადღება სჭირდება" (handoff) ბარათი ჩნდება და იხურება
10. Usage/tier ლიმიტები ჩანს; upgrade-request იგზავნება
11. ადმინ-პანელი: sellers, overview, upgrade-requests
12. `privacy.html`, `terms.html`, `delete-data.html?code=X` — იხსნება

---

## 🔙 ნაწილი 8 — უკან დაბრუნება

`main` ბრენჩი ხელუხლებელია მთელი მიგრაციის განმავლობაში.

⚠️ **მნიშვნელოვანი დაზუსტება:** `main.py`-ის fallback („თუ `dist/` არ არსებობს →
ძველი `frontend/`") გიცავს მხოლოდ იმ შემთხვევაში, თუ `dist/` **არ არსებობს**.
თუ `dist/` არსებობს, მაგრამ **გატეხილია** — fallback არ ჩაირთვება.

ამიტომ ავარიული გეგმა ორსაფეხურიანია:
1. სწრაფი: `git revert <commit>` → push → Render ავტომატურად აღადგენს
2. თუ ეს არ გამოდგა: წაშალე `frontend/dist/` და დაპუშე → fallback ძველ ფაილებზე გადავა

**შეამოწმე ეს ავარიული გზა ფაზა 1-ის ბოლოს, სანამ რეალურად დაგჭირდება.**

---

## 🎯 შეჯამება

| წესი | რატომ |
|---|---|
| MPA, არა SPA routing | 8 URL რეგისტრირებულია Meta-ზე/Supabase-ზე და გაგზავნილია კლიენტებთან |
| ფაზა-ფაზა, პატარადან დიდისკენ | თითო ნაბიჯი ცალკე მოწმდება |
| backend არ ვეხებით | API-კონტრაქტი უცვლელი |
| `dist/` git-ში | Render-ს Node არ აქვს |
| App Review-ს დროს არ ვდეპლოირებთ | განმხილველი გატეხილ აპს არ უნდა დახვდეს |
