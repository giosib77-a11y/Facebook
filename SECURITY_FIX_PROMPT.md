# 🔴 P0-1 — RPC-ების EXECUTE ნებართვები (მიგრაცია 0014)

> **სტატუსი:** დადასტურებული ემპირიულად 2026-08-23. გასწორება მიმდინარეობს.
> **წყარო:** მომხმარებლის მიგნება. კოდ-რევიუს (`CODE_REVIEW_PROMPT.md` v3) **გამორჩა.**
> **სიმძიმე:** P0 — გადამხდელი კლიენტის სერვისი ჩერდება, მონაცემი ზიანდება.

---

## 1. რა არის პრობლემა

`public.track_bot_customer` არის **`SECURITY DEFINER`** (`0004_subscriptions.sql:42`),
ე.ი. სრულდება ფუნქციის მფლობელის უფლებებით და **RLS-ს გვერდს უვლის.**

Postgres-ში ახლად შექმნილ ფუნქციაზე `EXECUTE` **ავტომატურად ეძლევა `PUBLIC`-ს.**
ჩვენს 13 მიგრაციაში **არც ერთი `REVOKE` არ არის** — შემოწმებულია `grep`-ით, 0 შედეგი.

შედეგად ფუნქცია გამოძახებადია **`anon` როლიდან**, PostgREST-ის `/rest/v1/rpc/`-ით.

### ემპირიული დადასტურება

```
POST /rest/v1/rpc/track_bot_customer          (anon key)
body: {"p_shop": "00000000-0000-0000-0000-000000000000", "p_psid": "probe"}

→ HTTP 409
  {"code":"23503","details":"Key (shop_id)=(0000…) is not present in table \"shops\""}
```

**კრიტიკული დეტალი:** შეცდომა **`INSERT`-ის მომენტში** მოვიდა, არა ავტორიზაციაზე.
ე.ი. ფუნქციამ იმუშავა და მხოლოდ განზრახ არარსებულ მაღაზიას დაეჯახა.
**რეალური `shop_id`-ით ჩანაწერი შეიქმნებოდა.**

---

## 2. თავდასხმის სცენარი

| # | ნაბიჯი |
|---|---|
| 1 | `anon` გასაღები **საჯაროა** — `config.js`-შია, GitHub-ზეც (ეს სწორია, ასე უნდა იყოს) |
| 2 | `shop_id` **საჯაროა** — `order.html?shop=<uuid>`-ში, რომელსაც ბოტი მყიდველებს უგზავნის |
| 3 | სკრიპტი იძახებს `track_bot_customer`-ს ყოველ ჯერზე **ახალი `p_psid`-ით** |
| 4 | `SECURITY DEFINER` RLS-ს უვლის → `bot_customers`-ში ახალი მწკრივი |
| 5 | `monthly_unique` აჭარბებს პაკეტის ლიმიტს |
| 6 | `webhook.py:179` → `over_limit = True` → **ბოტი წყვეტს პასუხს რეალურ კლიენტებზე** |

**ღირებულება:** უფასო მაღაზიის გასათიშად — **31 HTTP-მოთხოვნა.**
ბიზნეს-პაკეტისთვის — **3 001.**

### ⚠️ ჩვენი rate limiter ამას ვერ ხედავს

```
თავდამსხმელი → Supabase PostgREST → ბაზა
                      ↑
               ჩვენი backend აქ არ არის
```

`core/ratelimit.py` მხოლოდ **ჩვენს** endpoint-ებს იცავს. ეს მოთხოვნა ჩვენს
სერვერს **არასდროს ეხება.** არანაირი აპლიკაციური დაცვა არ მოქმედებს.

---

## 3. ზიანის რუკა

`bot_customers` სამ ადგილას მოქმედებს:

| სად | ფაილი | შედეგი |
|---|---|---|
| ბოტის ლიმიტი | `webhook.py:179` | **ბოტი ჩერდება** — სერვისის უარყოფა |
| გამყიდვლის usage-ბარი | `shops.py:148` | გაბერილი რიცხვი → **გამყიდველი ზედმეტ პაკეტს ყიდულობს** |
| ადმინის დაშბორდი | `admin.py:225` | ბიზნეს-მეტრიკა არასანდო ხდება |
| ბაზის ზომა | — | ყოველი გამოძახება = მწკრივი, გასუფთავების გარეშე |

**ყველაზე მზაკვრული მეორეა:** გამყიდველი ხედავს „780/800" და იხდის უფრო ძვირ
პაკეტში, რომელიც არ სჭირდება. ეს DoS-ზე უარესია.

---

## 4. სხვა ორი RPC — ზედაპირი ღიაა, ზიანი არა

`decrement_stock` და `apply_stock_delta` არიან **`SECURITY INVOKER`** (ნაგულისხმევი),
ე.ი. RLS მოქმედებს.

**შემოწმებულია უსაფრთხო ზონდით** — რეალური პროდუქტი, `anon`, `quantity: 999999999`
(რომ `UPDATE`-ის პირობა `quantity >= v_qty` ვერანაირად ვერ დაკმაყოფილებულიყო):

```
→ HTTP 400  {"code":"P0001","message":"PRODUCT_NOT_FOUND"}
   მარაგი ზონდის წინ და შემდეგ: 20 → 20 (უცვლელი)
```

`anon`-ს `auth.uid()` = `null` → RLS-მა პროდუქტი დამალა. **მონაცემი დაცულია.**

**მაგრამ ორივე გამოძახებადია** (ცარიელი სიით `HTTP 204`). ზედმეტი ზედაპირი
არავის სჭირდება — ისინიც უნდა დაიხუროს.

---

## 5. მიმდინარე მდგომარეობა — თავდასხმა ჯერ არ მომხდარა

```
bot_customers — სულ 4 მწკრივი
  voltbox     2026-08:  2 / 800  ✅
  voltbox     2026-07:  1 / 800  ✅
  chatassist  2026-08:  1 / 800  ✅

msg_count <= 1 მწკრივები: 0 / 4
```

თავდასხმის ხელწერა იქნებოდა: **ასობით უნიკალური `psid`, თითოეული `msg_count=1`.**
ასეთი ერთიც არ არის — ოთხივე ჩანაწერი რეალური საუბარია.

➡️ **სასწრაფო არ არის, მაგრამ ხვრელი ღიაა. სუფთა მდგომარეობაში ვასწორებთ.**

---

## 6. ⚠️ რა არ უნდა გატყდეს

გასწორების შემდეგ ეს სამი ნაკადი **უცვლელად უნდა მუშაობდეს** — სამივე
`service_role`-ით მიდის, ე.ი. `GRANT`-ი მას უნდა შერჩეს:

| ნაკადი | სად | კლიენტი |
|---|---|---|
| კლიენტების თვლა | `webhook.py:170` → `track_bot_customer` | `get_service_client()` |
| მარაგის დაკლება | `orders.py:_decrement_stock_atomic` → `decrement_stock` | `sc = get_service_client()` |
| მარაგის კორექცია | `orders.py:_apply_stock_delta` → `apply_stock_delta` | `get_service_client()` |

### 🚫 `set_updated_at`-ს არ ვეხებით

ის **trigger-ფუნქციაა**, არა RPC. PostgREST მას `/rpc/`-ზე არ ავრცელებს
(trigger-ის დამბრუნებელი ფუნქციები არ ექსპორტდება).

**და თუ მისგანაც წავართმევთ `EXECUTE`-ს `PUBLIC`-ს, trigger-ი შეიძლება
გატყდეს** ჩვეულებრივ `INSERT`/`UPDATE`-ზე. ის სიაში **არ უნდა მოხვდეს.**

---

## 7. გასწორება — მიგრაცია `0014`

```sql
do $$
declare f record;
begin
  for f in
    select p.oid::regprocedure as sig
    from pg_proc p join pg_namespace n on n.oid = p.pronamespace
    where n.nspname = 'public'
      and p.proname in ('track_bot_customer', 'decrement_stock', 'apply_stock_delta')
  loop
    execute format('revoke execute on function %s from public, anon, authenticated', f.sig);
    execute format('grant  execute on function %s to service_role', f.sig);
  end loop;
end $$;
```

**რატომ ციკლით და არა პირდაპირ:** `regprocedure` ხელმოწერასთან ერთად წერს
ტიპებს, ე.ი. ხელით არგუმენტების ჩამოწერა არ გვჭირდება და overload-იც სწორად დამუშავდება.

### სისტემური ნაწილი — განსახილველი, არა ავტომატური

```sql
-- ⚠️ არ გავუშვათ განხილვის გარეშე
alter default privileges in schema public
  revoke execute on functions from public, anon, authenticated;
```

**რას აკეთებს:** მომავალში შექმნილი ფუნქციები **ავტომატურად** დაიხურება.
ე.ი. იგივე შეცდომა აღარ განმეორდება.

**რატომ არ არის ავტომატურად ჩართული:**
`ALTER DEFAULT PRIVILEGES` მოქმედებს მხოლოდ იმ როლის მიერ შექმნილ ობიექტებზე,
რომელიც ბრძანებას უშვებს. თუ მომავალში Supabase-ის რომელიმე გაფართოება ან
dashboard-ი `public`-ში ფუნქციას შექმნის, ისიც დაიხურება — და ეს შეიძლება
რაღაც გატყდეს. **ჯერ ძირითადი გასწორება, დაკვირვება, მერე ეს.**

---

## 8. ვერიფიკაცია — სავალდებულო ნაბიჯები

### ა) `anon`-ს აღარ შეუძლია

სამივე ფუნქციაზე, `anon` გასაღებით:

```
მოსალოდნელი: HTTP 403 (42501 permission denied) ან 404
ᲐᲙᲠᲫᲐᲚᲣᲚᲘᲐ:  2xx ან 409/23503 (ეს ნიშნავს, რომ ფუნქცია ისევ სრულდება)
```

⚠️ **`23503` წარმატებად არ ჩაითვალოს** — სწორედ ის იყო ხვრელის მტკიცებულება.

### ბ) `service_role`-ს კვლავ შეუძლია

```
track_bot_customer + არარსებული shop → 409/23503  (ე.ი. სრულდება)
decrement_stock    + ცარიელი სია     → 204
apply_stock_delta  + ცარიელი სია     → 204
```

### გ) ცოცხალი ნაკადები (ეს მე ვერ გავაკეთებ)

| # | რა | ვინ |
|---|---|---|
| 1 | **ბოტს დაწერე Messenger-ში — პასუხი უნდა მოვიდეს** | მომხმარებელი |
| 2 | პანელში usage-ბარი იხსნება და რიცხვს აჩვენებს | ორივე |
| 3 | სატესტო შეკვეთა შესაკვეთი ფორმიდან (მარაგი უნდა დაიკლოს) | მომხმარებელი |

**#1 ყველაზე მნიშვნელოვანია:** `track_bot_customer` ბოტის გზაზეა. თუ `GRANT`
არასწორად შესრულდა, **ბოტი გაჩუმდება** — ზუსტად ის, რისგანაც ვიცავთ.

---

## 9. Rollback

თუ რამე გატყდა:

```sql
grant execute on function public.track_bot_customer(uuid, text) to public, anon, authenticated;
grant execute on function public.decrement_stock(uuid, jsonb)   to public, anon, authenticated;
grant execute on function public.apply_stock_delta(uuid, jsonb, int) to public, anon, authenticated;
```

ეს აღადგენს ზუსტად წინა (მოწყვლად) მდგომარეობას. **კოდი არ იცვლება, ე.ი.
deploy-ის დაბრუნება საჭირო არ არის.**

---

## 10. გასუფთავება — ამჯერად არ გვჭირდება

თუ თავდასხმა **მოხდებოდა**, მარტო ნებართვების გასწორება სერვისს **ვერ აღადგენდა**:
მრიცხველი `ym` ბაკეტშია, ე.ი. მაღაზია ლიმიტს ზემოთ დარჩებოდა **თვის ბოლომდე.**

მაშინ დასჭირდებოდა:

```sql
-- ⚠️ მხოლოდ თავდასხმის შემთხვევაში, ფრთხილად:
delete from public.bot_customers
 where shop_id = '<shop>' and ym = '<YYYY-MM>' and msg_count <= 1;
```

**დღეს ეს არ გვჭირდება** — §5-ის მიხედვით ყალბი მწკრივი არ არსებობს.
ჩაწერილია იმისთვის, რომ მომავალში ვიცოდეთ.

---

## 11. თანმხლები დოკუმენტაცია

| ფაილი | რა უნდა შეიცვალოს |
|---|---|
| `CODE_REVIEW_FINDINGS.md` | „P0 — 0" **არასწორია.** დაემატოს **P0-1** სრული აღწერით |
| `CODE_REVIEW_PROMPT.md` | გავლა 2-ს დაემატოს ფუნქციების `EXECUTE`-ის შემოწმება |

### რატომ გამორჩა რევიუს — მეთოდოლოგიური ხვრელი

გავლა 2-მა შეამოწმა RLS-ის პოლისები **ცხრილებზე** — ექვსივე, `WITH CHECK`-ების
ჩათვლით. მაგრამ **არასდროს შემოწმებულა, ვის აქვს `EXECUTE` ფუნქციებზე.**

`SECURITY DEFINER` სწორედ RLS-ის ლეგიტიმური შემოვლის მექანიზმია. პოლისების
დამოწმება მისი შემომვლელის დამოწმების გარეშე **არასრული შემოწმებაა.**

**გავლა 2-ს დასამატებელი პუნქტი:**

> **ფუნქციების `EXECUTE` ნებართვები.** ჩამოთვალე `public` სქემის ყველა ფუნქცია.
> თითოეულზე: ვის აქვს `EXECUTE`? `SECURITY DEFINER`-ია თუ `INVOKER`?
> ⚠️ Postgres ნაგულისხმევად `PUBLIC`-ს აძლევს `EXECUTE`-ს, ე.ი. `REVOKE`-ის
> გარეშე ყველა ფუნქცია `anon`-იდან გამოძახებადია PostgREST-ით.
> `SECURITY DEFINER` + `PUBLIC` `EXECUTE` = **RLS სრულად შემოვლილია.**

---

## 12. შესრულების წესი

- ⚠️ **მიგრაციას მომხმარებელი უშვებს** Supabase → SQL Editor-ში. მე წვდომა არ მაქვს.
- მიგრაციის გაშვებამდე **ვერიფიკაცია არ დაიწყოს** — შედეგი შეცდომაში შეგვიყვანს.
- **კოდის ცვლილება არ არის საჭირო.** ეს მხოლოდ ბაზის ნებართვებია.
  ე.ი. deploy-იც არ არის საჭირო (დოკუმენტაციის commit-ის გარდა).
- ცოცხალი ნაკადების შემოწმება (§8-გ) **მიგრაციისთანავე**, არა მოგვიანებით.
